// Phase 2 placeholder spec — the AppController no longer performs any routing.
// Full e2e tests for health and db-test endpoints will be added in the test/ directory.
describe('AppModule (Phase 2 placeholder)', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });
});
