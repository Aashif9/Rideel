// Placeholder — AppService is not used in the current Phase 2 architecture.
// All business logic lives in dedicated module services (HealthModule, DbTestModule, etc.)
import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService { }
